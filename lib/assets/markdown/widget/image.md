```` dart
            // ImageProvider
            Image(
              // 资源
              image: null,
              //
              semanticLabel: null,
              excludeFromSemantics: null,
              color: null,
              width: null,
              height: null,
              // 混合模式
              colorBlendMode: null,
              //缩放显示模式
              fit: null,
              //对齐
              alignment: null,
              //重复方式
              repeat: null,
              //当图片需要被拉伸时,centerSlice定义的矩形区域会被拉伸
              centerSlice: null,
              //类似于文字的显示方向
              matchTextDirection: null,
              //图片显示质量
              filterQuality: null,
              //图片发生变化,加载过程中是原图保留还是留白
              gaplessPlayback: null,
            ),
            Image.network(
              "",
              // 缩放
              scale: null,
              //其他与上同
            ),
            // 资源
            Image.asset(null),
            // 内存
            Image.memory(null)
````